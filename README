RFC 3489 + 5780.
为什么会有这个项目？

我们知道 RFC 5780理论上是更准的 NAT 测试方式，但目前互联网上有广泛的基于 WebRTC 的测试，例如 mao.fan，checkmynat.com。在不同的条件下，可能会产生不同结果。

经典的 NAT 分为四种类型：
1. NAT 1，全锥型，
2. NAT 2，IP 地址受限锥型。
3. NAT 3，端口受限锥型。
前三者都是 EIM，分别对应 EIF、ADF、APDF.
现实中的 ADF 非常稀有，根据小鲸社长统计只有大约 2/1000。如果直接获得公网 IP，但是有防火墙的，可能还有少量 Restricted Cone，但这并不妨碍真正的 NAT 2 极端稀有的事实。
4. NAT 4，对称型；还有俗称 4.5 的端口随机

有些时候，当端口发生多次变化时，基于 WebRTC 的原理会测不准 NAT 3，将传统 3489 模式下的对称型测为了端口受限锥型。但有时候这些东西用 5780 能测出不同的映射类型，有的可能是因为多次的 Nat，有的是因为内外端口变化，还有的是因为蜂窝的网关防火墙，可能只对同运营商不丢包之类。
同时，光武汉就有 汉口独立城域网（实体BRAS+小范围公网IP）、武汉市电信直连4134，以及云化、池化，备受争议的省网AS 137266（固移融合，电信的核心价值在宽带）.
本程序就旨在显示最原始的信息，通过长时间的实验来验证蜂窝基站以及各种网络的老化策略。

# NAT-Test

懒得写完整的README了，这个就当成一个杂谈吧。流水账，介意勿喷。

这个世界上对 NAT 有几大都市传说。
有的人认为 CGNAT 就一定是对称性。
有人非常悲观，认为万物皆对称。殊不知，除了蜂窝流量以外，基本上现实中的 NAT4 反而少。

目前广泛存在的几大误区：
1.有人以为公网才是 NAT1，然后所谓的全锥是2，普通是3，对称是4。
2.有人知道公网是 0，然后以为全锥是1，普通是2，对称是3。
3.还有的半吊子，使用了专业网站检测出来，普通的确实是 3，于是就开始脑补 2 是什么 UPnP 呀。甚至还有人脑补是 DMZ，拜托，DMZ 可是比 NAT1 还要开放的。

有的人陷入了万物皆对称型（4）的误区，然后以为网络……
NAT3 是宽松对称，NAT4 是严格对称。
几乎没有人知道 EIM 和锥型家族。
光猫拨号，也可以 DMZ 路由器。
IP 受限锥型极度稀有。
武汉电信蜂窝可是一种很特殊的 EIM + APDF，用 3489 会测出 4，但是用 5780 测出是 3。

按照 5780 的标准，理论上有 3×3=9 种。
但是由于实际中映射规则不可能比过滤规则严，所以排除掉那些以后，理论上有 6 种。

EIM + APDF 和 NAT3 的定义就应当是等价的。
个人认为 3489 和 5780 是工程。

而端口受限锥型、对称型，映射规则、过滤规则是理论定义。
而且如果我建立了端口映射，将内网端口映射到随机的外网端口。虽然这个外网本质上只是 CGNAT，并不是真正的公网，并不能改善现象。但是如果进行内外端口一致映射的话，3489 依然有概率显示对称型。但是 5780 会显示端点无关过滤，也就是传统的全锥。
武汉电信蜂窝的普通现代 STUN 映射路径稳定表现为 EIM；但 legacy RFC 3489 探测路径会为第二服务器端点稳定报告或建立一个相邻公网端口。


IP 受限锥型，按照规范的俗称应该叫做 NAT2，请注意区分。公网按俗称应该叫 NAT0，CGNAT 常见的是 NAT1，不是 2；家用路由器限制的端口受限锥型通常称之为 NAT3。除蜂窝网络以外，真正的 NAT4 很少。请不要悲观，请不要万物皆对称的固执退缩思维。
NAT2，又称之为万能 NAT，根据经典的 3489 四象限打洞表，公式可以简化为：小于 6 几乎必通，等于 6 大概率通。
NAT2，真正的 NAT2，IP 受限锥型 ADF 比大熊猫还要稀有。

按照旧标准的话，我总结了一套口诀。口诀就是小于6必通，等于6大概率通，大于6通不了，大于7不如买彩票。

NAT1不是UPnP或端口转发，只是有效期一定时间（例如特定固件实现可能为 5min）的UDP NAT状态表。


小于等于6可打通，大于6不行。
小于6一定绝对通，等于6几乎全通，等于7看软件牛不牛逼，大于7打通率还不如彩票。
严禁混淆 NAT1（Full Cone）、UPnP 与公网 IP 的概念。公网 IP 只是端到端的通行证，而 NAT1 是一种防火墙状态机策略（Endpoint-Independent Mapping/Filtering）。
论证为什么 UPnP 本质上是动态端口转发（Port Forwarding）
为什么即使在 NAT3（端口限制型）甚至对称型NAT4 的环境下开启 UPnP，其安全性也远低于关闭 UPnP 的 NAT1


NAT1 = Full Cone（全锥）
NAT2 = Address-Restricted Cone（地址受限锥）
NAT3 = Port-Restricted Cone（端口受限锥）
NAT4 = Symmetric（对称）

PS 的 Type 1/2/3 ≠ RFC 的 NAT1/2/3
本文提到的 NAT3 一律指 RFC NAT3（端口受限锥），非 Sony Type 3。

NAT2 是指和地址有关的过滤，不是全锥型，也不是消费级固件常见的 NAT3。NAT3 不是对称型。

理想中大致：
映射类型（M）：Ei AD APD Deny（阻止陌生的出站 UDP 连接）
过滤类型：EIF ADF PDF APDF Drop（阻止陌生的入站 UDP 连接）

端口号：（）内外一致、尽力而为（）随机（）
用户自定义方程〖当方程超出端口号范围时：（）内外一致（）随机
方程可选自变量包含端口号、内网 IP、MAC 地址等。除可进行初等数学运算外，还可以截取特定位字符〗

光猫默认 NAT3（Port-Restricted Cone）+防火墙
路由器开全局 NAT2，网络服务器开 NAT1（全锥型 Full Cone）
NAT1更适合个人之间的 P2P，在中继服务器很少资源下首次打洞，既可以任意联通，包括和 NAT4 用户。

UPnP并不是总是生效并且对外开放端口，更适合大规模（例如被利用成PCDN）
UPnP就是撅着屁股的0
NAT1适合我和哥哥点对点，直连不经中转

NAT2 对“两个人之间”的 P2P 很理想。
